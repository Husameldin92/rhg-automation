name: 👋 Hello Test - Learning GitHub Actions

# Only manual trigger for now (no schedule)
on:
  workflow_dispatch:  # This adds a "Run workflow" button in GitHub

jobs:
  hello-job:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📝 Say Hello
      run: echo "Hello! This is my first GitHub Action!"
      
    - name: 📅 Show current date
      run: date 